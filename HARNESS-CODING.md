# HARNESS-CODING.md — Harness Coding 通用规范

> **文档性质**：模型无关、项目无关的**通用方法论**（方法层）
> **核心**：有规划（Plan）→ 有测试（Test）→ 有反馈（Feedback）
> **定位**：给 AI 编码套上工程"挽具"（harness），取代无约束的 Vibe Coding；
> 使不同模型、不同工具、不同开发者产出**一致、可验证、可追溯**的结果。
> **最后更新**：2026-09-10
> **关联**：各仓库 `AGENTS.md`（项目入口层）· `docs/`（细节层）· `agents/`（本仓库源档案）

---

## 1. 什么是 Harness Coding

**Harness Coding** = 给 AI 编码套上工程"挽具"（harness）。

如同测试挽具（test harness）为测试固定运行环境，Harness Coding 为 AI 编码固定**工作流**：
先读规范、再动手、必有验证、结果回写——每一步都有明确的入口和出口。

| 维度 | Vibe Coding（无约束） | Harness Coding（本规范） |
| --- | --- | --- |
| 入口 | 一句需求，直接生成 | 先读规范 → 定位影响面 → 再动手 |
| 验证 | 跑不跑得通看运气 | 改动必有测试，跑通才算完成 |
| 闭环 | 一次生成即结束 | 结果回写文档/记录，形成闭环 |
| 一致性 | 每次结果可能不同 | 规范约束下，模型切换结果一致 |
| 可追溯 | 无记录 | 迭代记录 + 提交规范 + 文档同步 |

**核心是三要素闭环**：有规划（Plan）→ 有测试（Test）→ 有反馈（Feedback）。
任何编码任务都必须走完三要素，缺一步即视为任务未完成。

### 1.1 三层文档体系

Harness Coding 不依赖单一文档，而是一套三层文档体系（自顶向下，从通用到具体）：

| 层 | 文档 | 性质 | 内容 |
| --- | --- | --- | --- |
| 方法层 | `HARNESS-CODING.md`（本文件） | 通用，项目无关 | 工作方法论、行为准则、coding 约规、工作流模板、反模式、验收标准 |
| 项目入口层 | `AGENTS.md`（仓库根） | 项目专属入口 | 项目速查、目录结构、强制约定、文档同步映射、恢复上下文清单 |
| 细节层 | `docs/`（或等价目录） | 执行细节 | PRD、版本记录、架构/开发/软件规范、技能说明 |

**原则**：通用层不写项目细节，项目层不重写通用方法论；
切换模型时**先读上层、再读下层**，下层文档引用上层而非复制上层。

---

## 2. 三要素闭环

### 2.1 有规划（Plan）

1. **先理解，再动手**：读项目规范、目标页面/模块文档、现有实现，明确改动边界。
2. **定位影响面**：列出会触碰的文件、模块、文档、测试。
3. **分解步骤**：多步任务先建计划（todo 列表），逐步执行、逐步验证。
4. **选最小路径**：优先最小改动，不顺带重构无关代码。

### 2.2 有测试（Test）

5. **改动必有验证**：功能/接口/页面变更后，必须有对应测试或可执行验证。
6. **增量优先**：只跑受影响范围的测试；大规模重构/发版前才跑全量。
7. **验收可复现**：构建 + lint + 测试通过，才算完成；失败要定位到根因而非绕过。

### 2.3 有反馈（Feedback）

8. **结果可追溯**：把变更写入项目记录（迭代记录 / 变更日志 / 版本文档），而不是只在对话里说。
9. **文档随代码走**：功能/界面/接口/依赖变更，同步对应文档。
10. **提交有语义**：提交信息简短、语义化（Conventional Commits），便于回溯。
11. **失败要回报**：报错、失败、不确定处如实汇报，不隐藏、不猜测蒙混。

---

## 3. 通用行为准则（模型无关）

1. **不臆造**：不编造 API、参数、文件路径；不确定时先查证（读代码/文档/实测）。
2. **不越界**：不改与当前任务无关的文件；需要重构先说明并获确认。
3. **最小惊讶**：保持既有风格（命名、目录、语言、框架约定），不引入项目未使用的新依赖/新范式。
4. **多语言一致**（如项目要求 i18n）：面向用户的文案必须同步所有语言版本，禁止只加一种。
5. **权限边界**：提交、推送、发布、删除等不可逆操作，须用户明确指令后执行。
6. **如实汇报**：完成什么、改了哪些文件、验证结果、遗留问题，清晰列出。

---

## 4. 通用 Coding 约规

> 以下是**跨项目的默认编码约定**：项目自有约定（`AGENTS.md` / `docs/rules/`）优先；
> 项目未约定的事项，按本节执行。

### 4.1 总原则

1. **可读性优先**：代码是写给人看的；"容易懂"优先于"聪明"。
2. **单一职责**：一个函数/类只做一件事；职责膨胀就拆分。
3. **DRY**：不重复同一段逻辑；相同逻辑抽公共实现（但不对"看起来像"的三处过度抽象）。
4. **YAGNI**：不写现在用不到的功能；不为假设的需求过度设计。
5. **快速失败**：让问题尽早暴露；错误显式处理，不让错误静默传播。

### 4.2 命名约定

1. **表意**：名字说明"是什么/做什么"；避免单字母命名（循环变量 `i`/`j`/`k` 除外）。
2. **一致**：同一项目内严格沿用既有命名风格（最小惊讶），不混用多种风格。
3. **语言惯例**：
   - 变量/函数：跟随语言社区惯例（Python `snake_case` / JS `camelCase`）
   - 类/类型：`PascalCase`
   - 常量：`UPPER_SNAKE_CASE`
   - 文件/模块：小写（跟随项目：`kebab-case` 或 `snake_case`）
   - 布尔量：`is`/`has`/`can` 前缀（如 `is_valid` / `isValid`）
4. **名副其实**：名词用名词、函数用动词；布尔量用断言式命名（`isValid` 而非 `valid`）。

### 4.3 函数/方法约定

1. **长度受控**：单个函数只做一件事并做完；一般建议 ≤ 50 行（或遵循项目约定）。
2. **参数精简**：一般建议 ≤ 5 个；超过时封装为对象/结构体。
3. **纯函数优先**：无副作用、输入决定输出，便于测试与复用。
4. **边界校验**：输入校验在入口（API/CLI/对外接口）完成，中间层不重复校验。
5. **少嵌套**：嵌套一般 ≤ 3 层；用提前返回 / 卫语句拍平。

### 4.4 错误处理约定

1. **不吞异常**：禁止空 catch；捕获的错误必须处理、上抛或带上下文记录。
2. **错误带上下文**：错误信息说明哪里、什么、关键输入，能辅助定位。
3. **错误分级**：区分可恢复（重试/降级）与致命（终止并上报）。
4. **不外泄细节**：不向外部调用方暴露内部路径、堆栈、凭据等信息。

### 4.5 注释与文档约定

1. **注释解释"为什么"**：代码做什么由代码本身体现；注释写动机、约束、非显而易见的决策。
2. **公开接口必有文档**：API/CLI/技能等对外接口必须有说明文档。
3. **注释随代码走**：代码变更时同步更新注释，不留过期注释。
4. **TODO 带上下文**：`TODO` 注释说明为何遗留，必要时标注负责人/单号。

### 4.6 Git 提交约定

1. **Conventional Commits**：`feat:` / `fix:` / `docs:` / `refactor:` / `test:` / `chore:` 语义前缀。
2. **英文、简短、祈使句**：标题一句话讲清"改了什么、为什么"，不堆长度。
3. **一个提交一件事**：不混入无关变更。
4. **不提交坏代码**：提交点必须构建/测试通过。

### 4.7 依赖管理约定

1. **不擅自新增**：新增依赖须说明理由并获确认；优先标准库与项目既有依赖。
2. **单一声明**：依赖只声明在项目的单一来源（`pyproject.toml` / `package.json` 等），不散落各处。
3. **版本锁定**：锁定版本号，避免浮动版本导致环境漂移。

### 4.8 测试代码约定

1. **一个用例一个行为**：单个测试验证一个明确行为，不堆叠多个断言。
2. **命名描述行为**：`test_<模块>_<场景>`，名字能说明在测什么。
3. **结构清晰**：遵循 Arrange-Act-Assert（准备 → 执行 → 断言）。
4. **数据隔离**：测试数据与 mock 独立于生产数据；mock 放专属位置，不混入业务代码。

---

## 5. 标准工作流（七步模板）

```
1. 读规范     → 项目规范 + 目标页面文档 + 现有实现
2. 定影响面   → 文件 / 模块 / 文档 / 测试清单
3. 列计划     → 分解为可执行步骤（todo）
4. 实施       → 最小改动；保持既有风格
5. 验证       → 构建 + lint + 增量测试通过
6. 同步文档   → 页面/版本/架构/依赖/技能对应文档
7. 汇报       → 改了什么、验证了什么、遗留什么（不自动提交/推送）
```

任何任务，无论大小，都走完整七步；小任务可以压缩单步耗时，但**不允许跳过「验证」与「汇报」**。

---

## 6. 在项目里落地（如何使用本规范）

### 6.1 最小落地集（四步）

1. **放置 `HARNESS-CODING.md`** 到仓库根目录（方法层，通用内容，一般无需修改）。
2. **编写 `AGENTS.md`**（项目入口层）：项目速查、目录结构、强制约定、文档同步映射、恢复上下文清单。
3. **建立细节层** `docs/`（或等价目录）：PRD、版本记录、架构/开发规范。
4. **指定单一来源**：版本号、配置、命名等"单一来源"信息一律指向源文件，不硬编码可能过期的值。

### 6.2 恢复上下文清单（切换模型 / AI 助手时）

换一个 AI 助手 / 模型接手项目时，按此顺序确认上下文就绪：

- [ ] 读方法层（`HARNESS-CODING.md`）→ 工作方法论、行为准则与 coding 约规
- [ ] 读项目入口层（`AGENTS.md`）→ 强制约定 + 恢复上下文清单
- [ ] 读细节层 → 当前版本记录、目标页面 PRD、架构/开发规范
- [ ] 对照**单一来源**核对关键事实（版本号、端口、路径）；不信任旧文档里的过期数值
- [ ] 按项目约定启动开发模式，确认基线可用后再动手

---

## 7. 常见反模式（不要做什么）

| # | 反模式 | 正确做法 |
| --- | --- | --- |
| 1 | 不读规范直接生成 | 先读规范 + 现有实现，再定位影响面 |
| 2 | 顺手重构无关代码 | 最小改动；重构单独立任务并获确认 |
| 3 | i18n 只改一种语言 | 同步所有语言版本 |
| 4 | 自动 commit / push | 默认只改动 + 汇报，用户明确指令后才执行 |
| 5 | 跳过测试"在我机器上是好的" | 增量测试必须通过，验收可复现 |
| 6 | 隐藏失败、只报成功 | 如实汇报失败，附根因与影响面 |
| 7 | 硬编码易过期值（如版本号）进文档 | 指向单一来源文件 |
| 8 | 只在对话里说，不写回文档 | 同步回写项目记录与对应文档 |
| 9 | 吞异常（空 catch） | 捕获必须处理 / 上抛 / 带上下文记录（§4.4） |
| 10 | 硬编码魔法值（字符串/数字） | 抽成命名常量或配置项 |
| 11 | 随手起名、混用命名风格 | 沿用项目既有命名风格（§4.2） |

---

## 8. 验收清单（Definition of Done）

一个任务只有满足以下全部条目，才算"完成"：

- [ ] 已读规范、定位影响面、建立计划
- [ ] 改动最小、风格与既有代码一致
- [ ] 代码符合 §4 通用 coding 约规与项目约定（命名、错误处理、注释、提交信息）
- [ ] 增量测试 / 可执行验证通过
- [ ] 对应文档已同步
- [ ] 已如实汇报（改了什么 / 验证了什么 / 遗留什么）
- [ ] 未执行任何未获授权的不可逆操作

---

## 参考 / 关联（本仓库）

- **方法层**：本文件（`HARNESS-CODING.md`，通用，项目无关）
- **项目入口层**：[AGENTS.md](AGENTS.md) — benchscope 项目速查 + 全部强制约定 + 恢复上下文清单
- **源档案**：[agents/](agents/) — `Readme.md`（项目 harness 约定）· `Harness.md`（通用规范档案，本文件前身）· `Memory.md`（项目记忆）
- **细节层**：[docs/](docs/) — `prds/` · `versions/` · `rules/` · `skills/`