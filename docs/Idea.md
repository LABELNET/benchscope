!暂时不需要处理此文档，用于记录功能规划，暂不列入版本开发

1，插件功能：

  插件形式内置vllm和sglang配置和性能测试代码；
  插件有3种，GPU指标，测试参数，性能测试代码和精度测试代码；
  设置，插件功能GPU指标查询插件，做成通用skills，已经复制生成固定指标输出的插件，进行上传加载，代码和GPU日志也在统一目录下，内置maca和hpcc。插件开启会测试调用，若没有生效提示插件有问题开启不了，若开启，则显示GPU监控页面；若GPU监控页面有了，则真实性能测试，则测试完成，保存每个GPU的指标数据曲线到测试记录下面∶

  GPU 插件相关有如何实现；


2，文档与官网：

  官网托管 readthedocs? 还是 
  文档使用什么方案?

3，设置/模型 和 数据集 下载实现？还是只提供下载命令？

4，硬件相关
  - GPU 信息
  - 服务器信息: CPU/显存/内存
  - 服务器拓扑结构: common、等可视化显示 three.js

1，models/datasets 页面修改，整理数据（默认 hu ggleface 和 modelscope 都有，默认 face，可切换 modelscope和下载链接，可自定义本地模型和数据集;
2, 技能 发布流程和技能页面优化（一个按钮，中间弹出显示详情和操作按钮）
3，创建精度任务 界面优化；
4，测试精度测试，测试完毕后规划下面内容：
5，硬件监控(GPU 监控）、GPU插件、服务器信息、拓扑结构，关联性能任务和精度任务；
6，同步性能数据：安装模型和 GPU 型号双维度进行数据对比，硬件差异，性能差异，精度差异等，最终结论；
7，agents 测试规划；